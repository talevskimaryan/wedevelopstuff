const pinestelCard = document.getElementById("pinestel");
const taltechCard = document.getElementById("taltechteam");

VanillaTilt.init(pinestelCard, {
    max: 10,
    speed: 1000,
    transition: true
});

VanillaTilt.init(taltechCard, {
    max: 10,
    speed: 1000,
    transition: true
});
